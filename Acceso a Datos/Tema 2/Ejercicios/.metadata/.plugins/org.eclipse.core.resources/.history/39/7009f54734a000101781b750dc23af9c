package Ejercicios;

import java.io.File;
import java.io.IOException;
import java.lang.classfile.CodeBuilder.CatchBuilder;
import java.util.Iterator;

public class Ejercicio4 {
	
	public static void  main(String[]args) {
		
		File raiz = new File(Ejercicio1.RUTA);
		// creo el directorio en si
		File bin = new File (raiz, "bin");
		bin.mkdir();
		
		try {
			File rm =new File (bin, "rm");
			rm.createNewFile();
			File ls =new File (bin, "ls");
			ls.createNewFile();
			File cat =new File (bin, "cat");
			cat.createNewFile();
		}catch (Exception e) {
			// TODO: handle exception
		}
		
		File usr = new File (raiz, "usr");
		usr.mkdir();
		
		File dan =new File (usr,"dan");
		dan.mkdir();
		File bill =new File (usr,"dan");
		dan.mkdir();
		File gord =new File (usr,"dan");
		dan.mkdir();
		}
	
		try {
			for (int i = 1; i <=3; i++) {
				String nombre = "file"+i;
				File aux = new File(bill, nombre);
				aux.createNewFile();
				}
		}catch(IOException e){
			e.printStackTrace();s
		}
		
}
